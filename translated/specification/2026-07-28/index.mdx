---
title: 规范
---

<div id="enable-section-numbers" />

[模型上下文协议（Model Context Protocol）](https://modelcontextprotocol.io)（MCP）是一个开放协议，它实现了 LLM 应用与外部数据源和工具之间的无缝集成。无论你是在构建一个由 AI 驱动的 IDE、增强一个聊天界面，还是创建自定义的 AI 工作流，MCP 都提供了一种标准化的方式将 LLM 与它们所需的上下文连接起来。

本规范定义权威的协议要求，基于 [schema.ts](https://github.com/modelcontextprotocol/specification/blob/main/schema/2026-07-28/schema.ts) 中的 TypeScript schema。

有关实现指南和示例，访问 [modelcontextprotocol.io](https://modelcontextprotocol.io)。

本文档中的关键词 "MUST"、"MUST NOT"、"REQUIRED"、"SHALL"、"SHALL NOT"、"SHOULD"、"SHOULD NOT"、"RECOMMENDED"、"NOT RECOMMENDED"、"MAY" 和 "OPTIONAL"，当且仅当它们以全大写形式出现（如此处所示）时，应按 [BCP 14](https://datatracker.ietf.org/doc/html/bcp14) [[RFC2119](https://datatracker.ietf.org/doc/html/rfc2119)] [[RFC8174](https://datatracker.ietf.org/doc/html/rfc8174)] 中所述来解释。

## 概述

MCP 为应用提供了一种标准化的方式来：

- 与语言模型共享上下文信息
- 向 AI 系统暴露工具和能力
- 构建可组合的集成和工作流

该协议使用 [JSON-RPC](https://www.jsonrpc.org/) 2.0 消息在以下各方之间建立通信：

- **宿主（Hosts）**：发起连接的 LLM 应用
- **客户端（Clients）**：宿主应用内的连接器
- **服务器（Servers）**：提供上下文和能力的服务

MCP 从[语言服务器协议（Language Server Protocol）](https://microsoft.github.io/language-server-protocol/)中汲取了一些灵感，后者标准化了如何在整个开发工具生态系统中添加对编程语言的支持。以类似的方式，MCP 标准化了如何将额外的上下文和工具集成到 AI 应用的生态系统中。

## 关键细节

### 基础协议

- [JSON-RPC](https://www.jsonrpc.org/) 消息格式
- 无状态、自包含的请求
- 按请求的能力协商

### 特性

服务器向客户端提供以下任意特性：

- **资源（Resources）**：供用户或 AI 模型使用的上下文和数据
- **提示（Prompts）**：为用户提供的模板化消息和工作流
- **工具（Tools）**：供 AI 模型执行的函数

客户端可以向服务器提供以下特性：

- **征询（Elicitation）**：由服务器发起的、向用户请求额外信息的请求

### 额外的实用工具

- 配置
- 进度跟踪
- 取消
- 错误报告

### 扩展

在核心协议之外，MCP 定义了可选的[扩展](/extensions/overview)，它们添加模块化、专门化或实验性的功能。扩展始终是选择加入的，并需要客户端和服务器双方的显式支持，在初始化期间协商。值得注意的扩展包括：

- **[Tasks](/extensions/tasks/overview)**：长时运行操作的异步执行，带有轮询、执行中途输入和持久句柄
- **[Skills over MCP](/community/working-groups/skills-over-mcp)**：为智能体工作流提供的丰富、结构化的指令，通过 MCP 被发现和消费
- **[MCP 应用](/extensions/apps/overview)**：在对话中内联渲染的交互式 UI 元素（图表、表单、视频播放器）

## 安全与信任及安全

模型上下文协议通过任意的数据访问和代码执行路径实现了强大的能力。伴随这种能力而来的是所有实现者都必须仔细处理的重要安全与信任考量。

### 关键原则

1. **用户同意与控制**
   - 用户必须显式地同意并理解所有数据访问和操作
   - 用户必须保留对共享哪些数据和采取哪些操作的控制
   - 实现者应当为审查和授权活动提供清晰的 UI

2. **数据隐私**
   - 宿主必须在向服务器暴露用户数据之前获得显式的用户同意
   - 宿主不得在未经用户同意的情况下将资源数据传输到别处
   - 用户数据应当以适当的访问控制加以保护

3. **工具安全**
   - 工具代表任意的代码执行，必须以适当的谨慎对待。
     - 特别是，诸如注解之类的工具行为描述应被视为不受信任的，除非从一个受信任的服务器获得。
   - 宿主必须在调用任何工具之前获得显式的用户同意
   - 用户应当在授权使用某个工具之前理解它做什么

### 实现指南

虽然 MCP 本身无法在协议层面强制执行这些安全原则，但实现者**应当（SHOULD）**：

1. 在其应用中构建健壮的同意和授权流程
2. 提供关于安全影响的清晰文档
3. 实现适当的访问控制和数据保护
4. 在其集成中遵循安全最佳实践
5. 在其特性设计中考虑隐私影响

## 了解更多

探索每个协议组件的详细规范：

<CardGroup cols={5}>
  <Card
    title="架构"
    icon="sitemap"
    href="/specification/2026-07-28/architecture"
  />
  <Card
    title="基础协议"
    icon="code"
    href="/specification/2026-07-28/basic"
  />
  <Card
    title="服务器特性"
    icon="server"
    href="/specification/2026-07-28/server"
  />
  <Card
    title="客户端特性"
    icon="user"
    href="/specification/2026-07-28/client"
  />
  <Card title="贡献" icon="pencil" href="/community/contributing" />
</CardGroup>
